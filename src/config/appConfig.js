const appConfig = {
  defaultBlockId: 'A',
  refreshIntervalMs: 12000,
  slideshowIntervalMs: 7000,
  enableSlideshowByDefault: false,
  enableDisplayModeByDefault: false,
};

export default appConfig;
